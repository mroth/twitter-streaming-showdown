# install dependencies to run this on a Darwin machine (via homebrew)

# if you dont have langauges installed you will want the below as well
# install go
# install node
# install elixir

# will need this to get timeout (gtimeout on darwin)
install coreutils
